# 프로덕션 빌드(웹 배포 + Android 앱) 전용 — `vite build` 시 .env 위에 병합되어 이 값이 우선.
# (.env 의 VITE_SUPABASE_* 는 그대로 상속됨. 개발(`vite` dev)은 .env 의 localhost 사용)
# 앱은 Vercel 도메인이 아닌 로컬 origin에서 실행되므로 반드시 절대 URL(Railway)이어야 함.
VITE_SERVER_URL=https://gostopserver-production.up.railway.app
