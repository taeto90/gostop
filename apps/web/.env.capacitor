# Android 앱 빌드(`vite build --mode capacitor`) 전용 — .env 위에 병합.
# 앱은 로컬 origin(https://localhost)에서 실행되므로 서버는 절대 URL(Railway) 필수.
# (.env 의 VITE_SUPABASE_* 는 그대로 상속됨)
VITE_SERVER_URL=https://gostopserver-production.up.railway.app
